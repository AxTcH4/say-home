version: 3.8.0
services:
  say-db:
    image: mysql:8.0
    container_name: say-home-db
    restart: always
    environment:
      - MYSQL_DATABASE=say_home_db
      - MYSQL_ROOT_PASSWORD=root
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
volumes:
  db_data: